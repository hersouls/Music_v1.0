# ─────────────────────────────────────────────
# Moonwave Music — 환경변수 (전부 선택사항)
# 이 파일을 .env.local 로 복사한 뒤 값을 채우고 개발 서버를 재시작하세요.
#   copy .env.example .env.local   (PowerShell: Copy-Item .env.example .env.local)
# .env.local 은 git 에 올라가지 않습니다.
# ─────────────────────────────────────────────

# AI 자동 가사 싱크 (선택) — OpenAI Whisper 로 음원을 전사해 LRC 초안 생성
# 발급: https://platform.openai.com/api-keys
# 미설정 시 가사 싱크의 "AI로 자동 싱크" 버튼만 비활성, 나머지 기능은 정상 동작.
OPENAI_API_KEY=
